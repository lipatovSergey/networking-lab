## 🟩 Урок 8 — Маршрутизация, VPN и Tor (Конспект)

В этом уроке я изучил, как сетевой трафик на самом деле маршрутизируется в интернете и как VPN и Tor влияют на этот процесс.

### Основы маршрутизации

После разрешения доменного имени через DNS операционная система отправляет пакеты напрямую на **финальный IP-адрес назначения**, но при этом не знает полный маршрут.
Вместо этого ОС использует **таблицу маршрутизации** и передаёт весь нелокальный трафик на **gateway по умолчанию** (обычно домашний роутер).
Каждый маршрутизатор на пути принимает независимое решение о **следующем hop’е**, не зная полный маршрут до конечного сервера.

### Роль gateway

Gateway — это единственная сетевая сущность, на которую полностью полагается локальная машина.
Если IP-адрес не принадлежит локальной подсети, пакет отправляется gateway’ю, который затем передаёт его дальше в сеть провайдера и интернет.

### Как работает VPN

VPN не заменяет роутер и не заменяет интернет-соединение.
Он создаёт **виртуальный сетевой интерфейс** и изменяет правила маршрутизации так, что весь исходящий трафик направляется через этот интерфейс.

VPN-клиент:

- перехватывает трафик внутри ОС,
- шифрует его до того, как он достигает роутера,
- отправляет зашифрованные пакеты на VPN-сервер.

Провайдер видит, что VPN-соединение существует, но не может видеть содержимое трафика или конечное назначение.

На VPN-сервере:

- трафик расшифровывается,
- передаётся на конечный сервер как обычный интернет-трафик.

Для внешних сервисов IP-адресом источника является VPN-сервер, а не клиент.

### Ограничения VPN

VPN предоставляет:

- шифрование канала между клиентом и VPN-сервером,
- подмену IP-адреса,
- защиту от локальных сетей и провайдера.

VPN **не обеспечивает полную анонимность** и не защищает от трекинга на уровне браузера.

### Обзор Tor

Tor — это распределённая сеть узлов, управляемых добровольцами, предназначенная для анонимности, а не просто для шифрования трафика.

Трафик проходит через несколько узлов:

- Entry (знает клиента, но не знает назначение),
- Middle (не знает ни клиента, ни назначение),
- Exit (знает назначение, но не знает клиента).

Каждый узел снимает только свой слой шифрования, поэтому ни один из них не обладает полной информацией о соединении.

В отличие от VPN, Tor устраняет необходимость доверять одному провайдеру, ценой снижения производительности.

### Ключевые выводы

- Маршрутизация происходит hop-by-hop; ни одно устройство не знает полный путь.
- VPN шифрует трафик и изменяет маршруты, но требует доверия к VPN-провайдеру.
- Tor фокусируется на анонимности, распределяя доверие между независимыми узлами.
- VPN и Tor решают разные задачи и не являются взаимозаменяемыми.
